APEX SCALP GOD 6.8.1 — CONSISTENCY HOTFIX
TEST REPORT — 15 September 2026

HOTFIX TARGETS
1. COST AUTHORITY CONSISTENCY
   PASS — legacy 0–30m path-cost proxy no longer displays a bare PASS when broker costs are unverified.
   PASS — research proxy pass is explicitly labeled "RESEARCH PROXY PASS • LIVE VETO".
   PASS — only verified broker-cost inputs can display LIVE VERIFIED PASS.

2. CANONICAL NEWS COUNT CONSISTENCY
   PASS — LIVE NEWS RADAR badge and Relevant News field use the same canonical symbol-relevant total.
   PASS — High Impact count is calculated over the full canonical relevant set, not only the six displayed headlines.
   PASS — if more than six relevant headlines exist, UI says how many are displayed and confirms all are included in scoring.

3. BEST NOW RANKING CONSISTENCY
   PASS — ranking comparator is explicitly readiness-first: FIRE > PROOF > ARMED > WATCH > setup score.
   PASS — regression test confirms ARMED 60 outranks WATCH 90.
   PASS — within the same readiness state, higher setup score ranks first.
   PASS — UI now displays each candidate's rank priority and explains the ranking rule.

STATIC / PACKAGE CHECKS
PASS — node --check app.js
PASS — node --check sw.js
PASS — manifest.webmanifest parses as valid JSON
PASS — 313 unique HTML IDs; 0 duplicate IDs
PASS — 260 literal JS DOM references checked; 0 missing IDs
PASS — proof-era/storage keys preserved so 6.8 forward evidence and existing browser memory are not reset by this hotfix

IMPORTANT
These tests verify implementation consistency and fail-closed semantics. They do not prove trading profitability or future market direction.
