TITAN GOD 8.6 — STABILIZED GOD

- Preserves the V8 five-timeframe market brain: M5, M15, M30, H1, H4.
- Keeps the verified direct MT5-backed market feed and XAU spot cross-check.
- Rebuilt world-news layer: FreeNewsAPI (keyless/CORS) primary + GDELT fallback.
- News-provider failure no longer masquerades as “no news”; all-provider failure stays fail-closed.
- Signal-lock bug fixed: a temporary WAIT/news-provider issue does not automatically destroy a valid locked thesis.
- Data-error state now pauses the visible lock instead of showing a stale actionable signal.
- ISO/GDELT timestamp freshness parsing fixed.
- Static GitHub Pages compatible; no API keys required for core scans.

Safety: this app is analysis software, not a guarantee of profitable trades. Always compare the live price with your broker before execution.
